# immutable-chess

A TypeScript library for chess position representation and manipulation. This library provides an immutable data structure for representing chess positions, along with methods for querying and manipulating those positions.

## Installation

```bash
npm install immutable-chess
```
